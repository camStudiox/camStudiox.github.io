<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Cammdesigns | Custom Red & Black Creations</title>

<style>
/* ------------------------------
   GLOBAL THEME
--------------------------------*/
:root {
  --bg: #050505;
  --bg-alt: #111;
  --primary: #e50914;
  --primary-soft: #ff2a3c;
  --text: #f5f5f5;
  --muted: #b3b3b3;
  --border: #222;
  --radius: 14px;
  --max-width: 1200px;
  --transition: 0.2s ease;
  font-family: "Segoe UI", sans-serif;
}

body {
  margin: 0;
  background: #050505;
  color: var(--text);
  scroll-behavior: smooth;
}

/* ------------------------------
   HEADER / NAVIGATION
--------------------------------*/
header {
  position: sticky;
  top: 0;
  z-index: 50;
  background: rgba(0, 0, 0, 0.85);
  backdrop-filter: blur(12px);
  padding: 14px 24px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-bottom: 1px solid rgba(255,255,255,0.05);
}

.logo {
  font-size: 1rem;
  font-weight: 700;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

nav a {
  margin-left: 18px;
  text-decoration: none;
  color: var(--muted);
  font-size: 0.9rem;
  padding: 6px 10px;
  border-radius: 999px;
  transition: var(--transition);
}

nav a:hover {
  color: var(--text);
  background: rgba(255,255,255,0.06);
}

.discord-btn {
  border: 1px solid var(--primary);
  color: var(--primary-soft) !important;
}

/* ------------------------------
   HERO SECTION
--------------------------------*/
.hero {
  max-width: var(--max-width);
  margin: 60px auto;
  padding: 0 24px;
  display: grid;
  grid-template-columns: 1.3fr 1fr;
  gap: 40px;
  align-items: center;
}

.hero h1 {
  font-size: 3rem;
  margin: 0 0 12px;
}

.hero p {
  color: var(--muted);
  max-width: 480px;
}

.btn {
  display: inline-block;
  padding: 10px 20px;
  border-radius: 999px;
  text-decoration: none;
  font-size: 0.9rem;
  transition: var(--transition);
}

.btn-primary {
  background: var(--primary);
  color: #fff;
}

.btn-primary:hover {
  background: var(--primary-soft);
}

.btn-ghost {
  border: 1px solid rgba(255,255,255,0.2);
  color: var(--text);
}

.btn-ghost:hover {
  background: rgba(255,255,255,0.08);
}

/* ------------------------------
   SECTIONS
--------------------------------*/
section {
  padding: 60px 24px;
}

.section-inner {
  max-width: var(--max-width);
  margin: auto;
}

h2 {
  font-size: 2rem;
  margin-bottom: 12px;
}

p {
  color: var(--muted);
}

/* ------------------------------
   GRID CARDS
--------------------------------*/
.grid {
  display: grid;
  gap: 20px;
}

.grid-3 {
  grid-template-columns: repeat(3, 1fr);
}

.card {
  background: #0a0a0a;
  padding: 20px;
  border-radius: var(--radius);
  border: 1px solid var(--border);
}

.card h3 {
  margin-top: 0;
}

/* ------------------------------
   GALLERY
--------------------------------*/
.gallery-grid {
  grid-template-columns: repeat(4, 1fr);
}

.thumb {
  height: 160px;
  border-radius: var(--radius);
  background-size: cover;
  background-position: center;
  border: 1px solid #222;
}

/* Red/black gradient placeholders */
.thumb-1 { background: linear-gradient(135deg, #e50914, #111); }
.thumb-2 { background: linear-gradient(135deg, #111, #e50914); }
.thumb-3 { background: linear-gradient(135deg, #1a1a1a, #e50914); }
.thumb-4 { background: linear-gradient(135deg, #e50914, #1a1a1a); }

/* ------------------------------
   CONTACT FORM
--------------------------------*/
form input, form textarea, form select {
  width: 100%;
  padding: 10px;
  background: #0a0a0a;
  border: 1px solid #222;
  border-radius: var(--radius);
  color: var(--text);
  margin-bottom: 14px;
}

form input:focus, form textarea:focus, form select:focus {
  outline: none;
  border-color: var(--primary);
}

/* ------------------------------
   FOOTER
--------------------------------*/
footer {
  background: #000;
  padding: 40px 24px;
  border-top: 1px solid rgba(255,255,255,0.05);
}

footer .footer-inner {
  max-width: var(--max-width);
  margin: auto;
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
}

footer a {
  color: var(--muted);
  text-decoration: none;
}

footer a:hover {
  color: var(--text);
}

/* ------------------------------
